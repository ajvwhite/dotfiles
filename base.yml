# This playbook takes care of the base things,
# like bash, vim, and git
---
- name: Base setup
  hosts: localhost
  roles:
    - dotfiles
    - ohmyzsh
    - vim
    - git
    - composer
    - java
    - mactweaks
